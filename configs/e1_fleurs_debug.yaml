# E1 debug — tiny FLEURS slice on whisper-tiny/CPU (pipeline test only).
model_name: openai/whisper-tiny
device: cpu
dtype: float32
out_dir: activations/e1_fleurs_debug
store_full: false
dataset:
  loader: fleurs
  langs: [en_us, it_it]
  split: test
  n_per_lang: 5
  max_seconds: 10.0
